package com.fern.findaplant.database

import android.graphics.Bitmap

class PlantStorage {
    // Given the ID of an Observation, find the associated files in Storage
    public fun getImagesFromObservation(id: String): List<Bitmap> {
        return emptyList()
    }
}